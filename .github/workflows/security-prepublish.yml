name: Security prepublish

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  security-prepublish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run static security guardrails
        run: bash scripts/security-prepublish-check.sh
