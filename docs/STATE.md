# Project state

## Current milestone

**33 — Mr. Feast Face Retopology** is paused after the current visual result was rejected. A release-safety follow-up restores the original rigged model as the active, cache-busted runtime; the experimental appliance, reports, and Blender scripts remain dormant reference material only.

**34 — Basement Key Trail** remains preserved with automated acceptance complete and the subtle shelf-book discovery awaiting user playtest.

**35 — Player Mobility, Inventory, and Test Menu** is in progress with automated acceptance complete and sprint/crouch pacing awaiting user playtest. Mobile now has persistent hold-to-Sprint, Crouch, and Menu controls plus a touch-only Bag dossier button; all reuse the desktop-authoritative stamina/stance/menu/dossier state, preserve 44–48 px targets, and keep the lower control footprint within 24% of the phone stage. Persistent Sprint/Crouch yield while Feast Says owns movement. The intro-menu Load regression is fixed and cold-load covered: saves restore atomically without the front-door welcome, malformed payloads remain safely at the intro, and focused Chromium measured a 4.1 ms restore with an 18.1 ms next frame. Intro and Escape menus now fit without scrolling through 568×320 landscape, while definite stage sizing and guarded renderer updates prevent the canvas from expanding downward after Start or retaining a larger viewport height.

**36 — Camera Surveillance and Alarm Investigation** is in progress with automated acceptance complete and stealth timing, feedback, and Mr. Feast search tension awaiting user playtest.

**37 — Workroom Security Hub and Keypad** is in progress with automated acceptance complete and room layout, keypad feel, monitor readability, and ambience awaiting user playtest.

**38 — Estate Statue Trio** is in progress with implementation and automated acceptance complete; the creepy-elegant foyer pair and formal-garden fountain figure await user visual approval.

**39 — Mr. Feast Grounded Gait** is in progress with implementation and automated acceptance complete; planted-foot weight and pacing await user playtest.

**40 — Upper Window Gallery** is in progress with implementation and automated acceptance complete; the wider path and finished railing composition await user playtest.

**41 — Super Slop Character Animation Upgrade** is in progress with implementation and automated acceptance complete; the six-fighter Meshy/Blender roster and ability feel await user playtest.

**42 — Super Slop Mobile Max Screen** is in progress with implementation and automated acceptance complete; portrait/landscape control comfort awaits user phone playtest.

**43 — Readable Mansion Books** is in progress with implementation and automated acceptance complete; the physically titled 20-volume lore collection and printed-book marginalia clue await user playtest.

**44 — Mansion SFX Upgrade** is in progress with implementation and automated acceptance complete; final rain/thunder balance, footstep cadence, and interaction loudness await user playtest.

**45 — Tamper Distractions and Host Speech** is in progress with implementation and automated acceptance complete; tamper distraction pacing, bubble readability while hiding, and smalltalk tone await user playtest.

**46 — Caught in the Act** is in progress with implementation and automated acceptance complete; two playtest-reported bugs are fixed and regression-covered (a housekeeping visit silently overwriting the warning/catch, then missing basement trespass detection plus stuck roundabout chase pathing), and pursuit tension, warning tone, and the basement game-over flow await user re-playtest.

**47 — Mansion Contestant Conversations** is in progress with implementation and automated acceptance complete; Mara, Kip, and Juniper's room placement, visual readability, idle motion, and dialogue tone await user playtest.

**48 — Shared Seating and Contestant Routines** is in progress with the reported sofa-fit, regular-chair fit, statue-like seated-idle, repeated-seat cadence, Reading Room couch-height, tucked seated hands, reversed walking palms, frozen arms, thin/twisted Kip wrists, Juniper face shading, and nearby head-attention issues fixed and regression-covered. The player and three contestants share exclusive chair/sofa occupancy; Mara, Kip, and Juniper spend an estimated 66–71% of each authored ambient cycle seated, use planted procedural seated motion, must walk to a distinct hangout after standing, and smoothly glance toward nearby players only inside an anatomically limited forward field. Their arm layer now resolves each rig's signed palm/finger axes in world space: walking palms face inward toward the torso throughout the counter-swing, Kip's softer standing correction retains about 92% of his measured wrist-ring thickness instead of candy-wrapper pinching the skinned seam, while seated wrists stay roughly 27% down each thigh and the hands lie flat above the actual deformed thigh surface instead of collapsing into the inner-thigh seam. Mr. Feast's shipped `Head → headfront` direction visibly follows the player within the same bounded nearby-FOV rule; direct conversation pauses him for the complete spoken line, keeps tracking the player's live position, and smoothly brings the torso around before the head reaches its limit. Security alarms and witnessed trespass still preempt that pause. Final seating/routine feel awaits user re-playtest.

**49 — Workroom Keycode Scratches** is in progress with implementation and automated acceptance complete. The reported clue occlusion and printed-lettering look are now fixed with carrier-only stronger tilts, raised-corner placement, and hand-traced gouge paths; player-facing toast and dossier language now preserves only the raw Painting Room marks, so players must infer their purpose and ordering themselves. Discovery pacing and final scratch readability await user re-playtest, along with the fixed unlocked-keypad digit echo.

**50 — Front-Door Show Welcome** is in progress with implementation and automated acceptance complete. A fresh entry now stages Mr. Feast just inside the foyer for a seven-line reality-show briefing about the one-million-dollar prize, public areas and cameras, locked doors, and the forbidden basement. The first visible line now includes an immediate touch/keyboard-accessible `Skip intro` action that releases the welcome without changing story, inventory, or security state. Its first-load path is progress-aware after controlled Chromium reproduced false retry errors from both legacy fixed startup deadlines; final tone and reading pace await user playtest on desktop or phone.

**51 — Crouch Stealth Meter** is in progress with implementation and automated acceptance complete. Crouching now shows a 0–100 concealment meter driven by real sampled room light and recent movement, and a crouch-gated effective visibility scales Mr. Feast's witnessed-sight range (down to a `2.6m` fairness floor) and camera acquisition while every standing baseline stays byte-identical; meter readability and dark-evasion tension await user playtest.

**52 — Feast Says First Competition** is in progress with implementation and automated acceptance complete; user playtest remains. The first post-welcome exploration window still ends after ten active minutes or immediately after the first XIII book, concealed shovel, or Painting Room scratch discovery. That discovery remains earned and later clue progress pauses. The old Ballroom report sign is replaced by a film-production set with a long-lens cinema/broadcast rig, two studio lights, a boom microphone, and Mr. Feast waiting at its center. Its Feast-only placement keeps the lens roughly `0.75m` farther from the player/contestant lineup, while the locked lens and both physical lamp faces stay trained on that lineup throughout the live round. A five-active-minute call timer begins once the required host/cast interaction is available; blocking UI pauses it, and a no-show produces the recoverable `ELIMINATED` screen. The player must face Mr. Feast and use E or touch Interact to begin. Called saves preserve the exact remaining time, while briefing/command/result saves normalize to the called set with a fresh five minutes. Mr. Feast's speech is now the sole instruction channel: he teaches the real rule — obey only instructions beginning with `Feast says`, with the lowest score eliminated — and that initial explanation offers an immediate `Skip rules` action that begins command one without skipping live gameplay. He speaks every call, gives a correct/incorrect verdict, and later cuts off the contestants' anxious or funny replies. The interface retains only a thin Feast Says / round / timer strip and never duplicates a command, hint, score, or standings. The fixed sequence remains genuine step left, plain right-step fake-out, genuine step back, plain crouch fake-out, distrust point, and sacrifice step; the two fake-outs require stillness, distrust uses look plus E/touch Interact, and sacrifice reads actual world displacement. Every call allows 7.5 seconds and the expanded 7.2-second result phase accommodates Mr. Feast's verdict, contestant banter, and warning. Mara, Kip, and Juniper retain distinct choices and planted response motion. Authored scoring preserves Kip as the first NPC elimination when the player survives. Kip remains visible in a slumped upset pose for a pathetic plea and Mr. Feast's ominous answer while Mara and Juniper walk back to their routines; a significant offscreen break later removes Kip and releases Mr. Feast to patrol.

**53 — Mansion Ambient Detail Pass** is in progress with implementation and automated acceptance complete. A whole-estate decor sweep now fills the barest views: clipped foundation beds with limestone curbs flank the portico, low shrub pairs line the driveway lawn edge (deliberately skipping Mr. Feast's z≈29.5 gate response spot), and stone planter urns frame the entry; the dining table carries a runner, ten place settings, twin candelabra, and a fruit centerpiece; kitchen counters gain working clutter that keeps the sink span and burner interactions clear; all four upper suites receive collidered nightstands with per-suite accents, foot-of-bed travel trunks, and bedside runners off the z≈±6 patrol lanes; and the laundry (drying lines, washtub, baskets), wine cellar (tasting set, hooped barrels), bulk storage (stacked crates, barrels, tarp pile), boiler room, music room, library, and rear lounge each get one small vignette. No new shader lights, interactions, or tamper targets; `scripts/test-mr-feast-ambient-details.mjs` pins the layout constraints, and 73-view before/after captures live in `output/iterate/mr-feast-ambiance-pass/`. A user-directed second pass then unified every below-grade ceiling fixture on the utility cage-and-bare-bulb look (basement corridors and the service-stair landing lose their chandelier rings through a geometry-only branch; the pinned corridor cone emitters and ring-shadow contract are untouched) and added mantel decor to all three fireplaces, vanity counter sets in both bathrooms, foyer console vases, an overhead service pipe along the rear cross-corridor (chase lane clear), a pool-deck drinks table, a portico entry mat, and rear-terrace planter urns. A third texture/detail pass upgraded the small-item fidelity: procedural grocery-label canvas textures on shelf stock (bread/baskets stay plain), a semantic cold-larder refrigerator (milk, butter, jam, roast, cheese wedges, lettuce, egg tray), marble pool-stair treads with riser reveals and collider-free brass handrails, and a storm-water shader with rain ripple rings and drifting grain. The pool then doubled its swimming area westward (water 9.7→19.4 m wide, centre x -13.85) with the entry stair/ramp pinned at the authored x=-9 (`stairX`), re-cut west grounds, wrapped decks/coping/supports, four lane inlays, an aspect-corrected rain shader, a relocated estate tree, and a widened POOL TERRACE zone; all four authored pool routes plus new west-deck/west-floor walks re-pass under deterministic physics stepping. A final no-empty-cabinets sweep added nine role-specific stock kinds (barware, sideboard, cookware, prep, undersink, cellar-reserve, linens, tools, washroom) to every bare cupboard with `interiorLight: false` preserving the light budget; a live-scene audit proves all 25 storage pieces carry visible stock, and the static suite now fails if any future cabinet ships without stockKind or a dressed walk-in interior. A follow-up fix split the doubled pool's north terrace paver around the stair mouth (it had capped the stairwell as one solid slab); the stairs now read as an open recess and all six pool walks still pass. A further fix extended the single water plane north (to z=−18.40) so the submerged lower steps read as underwater instead of dry with a gap; the opaque north wall, coping, deck, and top step hide the overshoot while the water stays visible through the stair gap as one seamless surface (the shader uv aspect is now derived from width/depth), and all colliders and pool routes are unchanged. Mr. Feast's opening-welcome staging position then moved from z=8.15 to z=7.9, halving his distance to the foyer chandelier's downlight center so his face reads brighter (confirmed with a live pixel-luminance sample: 27.0 → 30.7, a real ~13.5% increase, not just a screenshot impression) while staying well inside the welcome test's distance bounds. The two foyer card trays now use a 0.14m signed inset (rather than the overhanging 0.35m offset); live mesh bounds confirm both x=±3.76 trays sit wholly inside their x=±3.9 rotated console tops, with 0.023m side margins. The rear ballroom entry then lost its raised terrace landing and approach ramp: the door now opens onto the terrace over a flush limestone doorstep, the floor-level collision deck is trimmed back to the rear wall so no invisible floor floats over the pavers, and a short buried (invisible) ramp carries the ~0.2m rise because bare autostep does not reliably lift the rounded player capsule over a ground-butting step. Mr. Feast is unaffected (authored node-height ramp segment; the buried ramp is a trimesh invisible to his fixed-box check), and deterministic stepping proves the player walks up into the BALLROOM and back out onto the REAR LAWN. Prop tone, fridge readability, and pool-entry feel await user playtest. Notes: `test-mr-feast-tamper-distractions.mjs` currently fails at the committed Storm Run baseline itself (dispatch held by the new competition during the test window — reproduced in a clean worktree at d812e117 with no ambient-pass changes); it belongs to the Storm Run milestone. Separately, `test-mr-feast-opening-welcome.mjs` cannot currently run at all — its `startWelcome()` waits for Mr. Feast's character load before clicking Start, but load only begins from inside the Start-click handler (`scheduleOptionalCharacterLoads`), so the wait never resolves; this is pre-existing (file untouched since the original welcome commit) and unrelated to the host-position change, which was instead verified by replicating the suite's exact diagnostic assertions against the corrected click-then-wait sequence.

**54 — Storm Run Second Competition** is in progress with implementation and automated acceptance complete. Game 2 now remains locked until Feast Says is complete and the player either discovers all four keypad digits behind the Painting Room frames or recovers the hedge-maze basement key; elapsed exploration time, unrelated clues, and partial digit sets cannot call it. The old rear-terrace report sign is replaced by the same film-set grammar: one long-lens cinema/broadcast rig, two studio lights, a boom microphone, and Mr. Feast waiting at its center. The player has five active minutes to face and interact with him; blocking UI pauses the deadline, incomplete host/cast loading does not charge it, and a no-show produces a recoverable `ELIMINATED` screen. Called saves preserve the exact remaining time, while briefing/running saves return to the set with a fresh five minutes. A called-save restore also preserves competition ownership of Mr. Feast: the general post-load recovery cannot restart patrol or switch him to the stalk cycle while he is staged, so his waiting pose remains fully planted until interaction; beginning the briefing still releases only that pose freeze and restores his living idle. Mr. Feast explains the race without a `Beat Mara` objective and offers an immediate `Skip rules` action that preserves the full spoken `3–2–1`; he releases the contestants with only `Run!` and never announces a checkpoint or landmark. The held briefing temporarily lifts the existing hemisphere, re-aims the existing moon from the player side, and raises exposure so his face reads clearly; it restores the regular storm-dark direction and values at release. The thin progress/time strip, twelve visible blue breadcrumbs, collection chimes, and stamina-fair contestants remain the complete route guidance. The north-maze phone route retains its measured readability. Three independent lightning events match the annotated property map: northwest trees after the player reaches the north garden/front-yard edge, northeast trees when the player is close to the front door, and a life-size reveal at the far end of the maze's final straight. The last event arms as the player turns south at `(22,-0.25)`; Mr. Feast stands `13.5m` ahead at `(22,-13.75)`, immediately before the westward exit turn, facing north toward the player. Both rear-exit practicals stay zero-energy from race release through the complete lightning reveal without changing shader topology, and the available fixture restores only after he disappears. Crossing a trigger persistently arms its one-shot for the bounded route window; Mr. Feast must then be inside the real camera view, pass a three-dimensional `0.90` facing dot, and have a clear torso ray. Looking away, down, or up stays silent, including immediately after leaving the small trigger. All three silhouettes use believable human model scale; the first two now lurk roughly `1.7m` beside real tree trunks without intersecting their colliders, and retain phone readability through a separate wider flash-only fill rather than tree-sized models. Runners remain visible and the still-present checkpoint briefly lowers its glow. The flash fill remains shader-resident at zero intensity outside the bolt, letting the briefing warm the light/material variants so the first strike does not change renderer point-light topology. The close-bolt audio schedules on the Web Audio clock, skips each recording's quiet lead-in, uses a `2.1x` Storm Run multiplier, and holds a roughly `16dB` rain duck; its former white-noise crack is removed so no static-like transient precedes the recorded thunder. A temporarily interrupted mobile context still queues that close strike until the next trusted gesture instead of dropping it. A player win holds a witnessed back-door aftermath instead of popping the cast away: Mr. Feast delivers a sharper elimination line, Mara covers her face with both hands and warns that the lightning sightings were Mr. Feast counting survivors, and Juniper walks through the rear terrace door and central stairs to her Reading Room routine. Juniper's first later conversation adds a one-time explanation of the impossible sightings, persisted across save/load. Mr. Feast's authored back-door mark and visibility are continuously resynchronized, and stepping into the nearby rear lounge cannot reset him. Only being at least sixteen metres from the finish while no longer viewing it removes Mara and releases Mr. Feast to patrol. Clues reopen on completion; a race loss remains recoverable. Final route feel, ending tone, and scare impact await user re-playtest.

**55 — Tardigrade Creature and Prop Asset Upgrade** is in progress with implementation and automated acceptance complete. After the user rejected the striped/grub-like second hero, a final anatomy audit also rejected v3 because its mesh showed five planted rows despite a four-pair rig. A corrected concept derived from the supplied screenshot visibly exposes exactly four near-side legs and drove the accepted Meshy image-to-3D v4 master: uniform pale peach skin, six deep rounded folds, a circular oral tube, exactly four planted leg pairs, and fine ivory claws. Blender 4.5 LTS cleaned and reduced it to 13,881 triangles, independently measured four source rows before rigging, added a browser-readable non-tintable dark oral inset, retained `SkinPrimary` store tinting and all four attachment sockets, authored the `idle`, `scuttle`, `dash`, `curl`, and `airborne` clips, and replaced the old upright curl with a compact leg tuck and shallow body arc. The full eight-asset uncompressed roster is 8.348 MiB and still preserves the existing JavaScript roots, physics, goals, progression, saves, and per-instance procedural fallbacks. Hardened static and real-browser QA covers exact user/input hashes, rejected-source locks, measured runtime leg rows, Meshy task provenance, finite GLB data, budgets, pose variation, no root translation, socket hierarchy, clone/resource safety, all eight live instances, desktop and 390×844 rendering, 44px touch targets, and deliberate ciliate failure fallback. Final hero appeal and animation feel await user playtest.

**56 — Basement Flashlight and Security Risk** is in progress with implementation and focused automated acceptance complete. An emergency-style flashlight is visibly cradled on the Archive-side landing about `2.4m` from the service-stair bottom; E/touch adds one persistent Bag item, F or the mobile `Light` button toggles it, and load always restores it safely off. Activating it now shows only the beam, with no lower-right carried model. Its warm `74`-intensity, `8.2m`, `0.38rad`, shadow-free cone is wall-clamped and shader-resident inside the fixed six-spot budget, while inactive state performs no recurring pose/raycast work. Active use imposes a `0.72` light-exposure floor on crouched stealth and, when a clear same-floor camera can plausibly see the player, emits one recoverable `flashlight-use` event that flashes `Flashlight spotted` and sends Mr. Feast through responding/searching/returning/patrol without direct pursuit or permanent lockdown. Pickup noticeability, beam balance, and tension await user playtest.

**57 — Mr. Feast Pursuit Evasion, Pathing, and Footsteps** is in progress with implementation and automated acceptance complete. Mr. Feast now emits spatial wood/stone/grass footsteps at the sampled grounded contacts of his shipped stalk and run clips, with distance attenuation, stereo pan, darker distant filtering, master-mute ownership, and phase/foot/surface diagnostics. Pursuit owns a last-known point refreshed only by personal sight, clear close-range basement awareness, or hostile camera tracking: unseen movement cannot retarget him, the loss timer drains even while he is moving, and a real hiding spot ends pursuit within `3.4s`. The reported inert point-blank basement encounter is fixed: a clear, non-hidden trespasser within `2.35m` is noticed even beside or behind him, while walls, other floors, and hiding remain safe. A second reported basement defect is also fixed: once a legitimate detection gives him a cross-floor route, periodic pursuit refreshes retain the authored stair/ramp leg until he reaches its destination floor instead of re-anchoring him to the upper landing. Clear visible same-floor chases steer straight through collision-aware movement for the full `9m` sight range, while stalls, walls, stairs, and doors retain the shortcut graph fallback. Final footstep weight, hiding tension, and pursuit feel await user playtest.

The current contestant repair preserves GLTFLoader's already converted Y-up orientation before runtime fitting, avoiding the erroneous second axis rotation that enlarged the cast and laid them sideways. It filters the source walk arm tracks in favor of the established relaxed arm pose and moves contestants and Mr. Feast through fixed-furniture character collision with floor-route steering while preserving authored stair travel. Focused QA asserts upright human-scale contestant bounds and proves both kinds of NPC are blocked by a furniture-height obstacle.

**2026-07-21 competition-staging follow-up:** Feast Says now uses a long-lens cinema pedestal rig, set roughly `0.75m` farther from the fixed player/contestant lineup than the shared placement while retaining the host-clearance contract. Its lens and both physical studio-light faces remain locked on that lineup through briefing, commands, and results. The lineup positions are now logic-only, with every colored floor ring and circular action pad removed while the existing movement, scoring, and camera framing remain unchanged. Storm Run still keeps Mr. Feast facing the back door while waiting for interaction, then holds the player-facing briefing pose through the countdown; only during that presentation, its existing hemisphere, moon, and exposure lift makes him readable, with the moon returning to its normal storm direction at `Run!`.

**2026-07-21 Storm Run planted-host follow-up:** The reported walking-in-place intro came from the looping idle body clip continuing to advance while Storm Run pinned Mr. Feast's root at the back-door set. Only the pre-interaction waiting phase now pauses the body action on a named authored idle frame. Starting Storm Run clears that override so his normal living idle resumes while he explains the rules and counts down, with the staged root still fixed; facial expression, blinking, and bounded head attention remain independent in both phases. The first runtime fix shipped under the mansion page's previous script query, however, so existing browser sessions could retain the pre-fix JavaScript for GitHub Pages' ten-minute cache window. The page now requests `20260721-storm-run-wait-pose-1`, and the focused regression pins that release key while sampling six irregular waiting frames to prevent a full animation cycle from masquerading as a planted pose. It measures `0m` maximum waiting-foot travel, `0.134972m` of briefing body motion, and `0m` of briefing root drift, with no runtime-console errors; proof is `output/playwright/mr-feast-storm-run/storm-run-{planted-wait,animated-briefing}-desktop.png`. Renovation invariants pass. The current full Storm Run worktree test is separately blocked after its intentional no-show because an in-progress uncommitted test edit removed the required fresh-page reload, and the adjacent Contestant 13 run timed out in its current optional-character readiness path before reaching this animation slice.

## Stable baseline

- Feast Says now provides the first real competition/investigation ceiling. Its ten-minute clock begins only after the welcome and consumes capped wall time instead of physics-clamped frame time; existing pause surfaces stop it. The first qualifying clue can call it early without losing its own feedback, then all undiscovered story carriers, keypad progression, camera enforcement, and tamper-housekeeping dispatch remain held through the competition. The call replaces the report sign with a camera/two-light/boom production set around Mr. Feast and gives the player five active minutes to interact with him; blocking UI and unavailable required character assets pause the deadline, while a no-show eliminates the player. Called saves preserve the exact time and transient competition saves safely restart the call at five minutes. Mr. Feast's bubble explains the obedience rule, delivers all six physical/psychological calls, judges each response, and warns the cast; no written command, choice hint, score, or standings duplicates him. A thin event/round/timer strip remains for essential status. Each call uses a 7.5-second window and 7.2-second result hold for verdict, contestant banter, and warning, with distinct NPC targets and isolated planted response posing. The surviving aftermath holds Kip's upset pose and two-line exchange while Mara and Juniper return to their routines, preserves one post-game debrief line per survivor, and defers Kip's disappearance plus Mr. Feast's patrol release until an offscreen break.
- Storm Run provides the second competition ceiling without mixing clues into the minigame. Its call uses the same camera/two-light/boom set and waiting-host interaction on the rear terrace, with the same fair five-active-minute no-show deadline and save normalization. Mr. Feast's bubble explains the checkpoint and elimination rules without a `Beat Mara` objective, speaks `3–2–1`, says only `Run!` at release, and speaks the outcome; he never announces checkpoints. A thin progress/time strip, twelve visible breadcrumbs, and audible chimes carry all route feedback. The north maze reuses the fixed exterior light budget to keep its phone route readable. Exactly three human-scale, non-hazard close-bolt reveals match the annotated northwest-tree, northeast-tree, and final-maze-straight positions; the last one arms at the first southward cell and places a north-facing Mr. Feast at the far end immediately before the westward exit turn, while the exit remains dark until the flash and apparition end. Each arms on proximity, waits for an actual on-screen three-dimensional view and clear sightline, uses the louder interruption-safe trimmed/ducked recorded thunder without the rejected white-noise crack plus a separate local silhouette fill, then disappears with darkness under the same `1.2s` cap. A winning back-door ending keeps Mara visible with both hands covering her face for her sad final line while Juniper physically walks home. Mr. Feast remains pinned visible even when the player steps through the back door; Mara's removal and his patrol release require both a sixteen-metre separation and an occluded/offscreen view, matching the first competition's witnessed-loss grammar without showing a death scene.
- Playable chain: subtly misfiled Library shelf book → rose-hidden hand shovel → B-13 basement key beneath faint XIII at the deepest hedge-maze dead end → locked Kitchen service stair → Archive evidence cage and recording → keypad-gated Workroom patron-feed sabotage. The Workroom PIN `0513` is now canonically discoverable in-world through the Painting Room scratch hunt.
- Four Painting Room paintings — one per wall (`Five Doors, No Hallway`, `A Very Polite Eclipse`, `The Garden Has Too Many Knees`, `The Choir Beneath the Floorboards`) — each conceal a pale plaster scratch of a Roman numeral (`I`–`IV`) and its digit. These four carriers now roll `0.32rad` while ordinary portraits retain `0.17rad`; each fixed wall mark follows the alternating bottom corner that rises, and its inner edge is regression-proven at least 94% clear of the tilted rail. The glyphs are deterministic hand-traced paths with broken gouges and fibers rather than canvas-font lettering. Tilting remains the existing risky tamper interaction, so hunting the marks invites Mr. Feast's housekeeping visits and witnessed/recorded pursuit.
- Each first reveal fires a neutral discovery toast and updates one evolving `Painting Room notes` evidence-pad entry that preserves found numeral–digit pairs with `??` for unknowns. The fourth discovery retains all four raw pairs without naming a keypad, Workroom, PIN, or code, or assembling `0513`; players infer the connection themselves. Discoveries persist through explicit save/load (scratch visibility resyncs to the reloaded untampered paintings — loading also straightens all tampered decor now), reversible Dev Mode grants and restores the hunt, and a fresh run starts clean.
- Fixed the reported dead keypad: a save that had already unlocked the Workroom reopened the pad in the accepted state where digit presses were silently swallowed; digits now always echo on the display while submit keeps reporting `ACCESS GRANTED` on an unlocked pad.
- A fresh non-QA entry now begins at the closed front-door threshold with Mr. Feast visibly staged `2.05m` ahead in the foyer. Movement, ordinary interactions, patrol, and gameplay-affecting menu actions remain gated while seven complete speaker-labelled lines establish the reality show, one-million-dollar prize, permitted rooms and grounds, camera behavior, locked-door rules, and a cryptic basement prohibition. Each line holds for a named 6–10 second reading window and progresses automatically, while E/tap becomes an optional Continue action only after `2.4s`; completion clears the bubble, unlocks exploration, and resumes his foyer patrol without revealing or mutating Contestant 13 state.
- The page shell now allows a `60s` inactivity window for the cache-busted 1 MB runtime and refreshes it as each dependency/runtime phase arrives. The runtime uses a separate `45s` inactivity watchdog rather than a fixed total-duration deadline; named loading phases plus texture and 13 MB estate-statue transfer progress refresh both timers. A real silent initialization still exposes the existing Retry action, while normal cold downloads no longer flash an error and then become ready moments later. `render_game_to_text()` reports the active startup timeout and activity count.
- The middle Library case now omits one generated volume so the clue book occupies a clean shelf gap rather than overlapping it. The off-tone spine is pulled about `0.07m` ahead of its neighbors, carries a small zero-depth `XIII` made from broken layered gouge/fiber strokes with a full corner-to-corner crossed `X`, and displays one seeded title borrowed from the ordinary 20-book catalog.
- Reading or rereading that copy uses the shared parchment reader: its selected title, author, and lore excerpt remain normal printed content, while a separate hurried blue handwritten margin note points to the formal-garden shovel and hedge-maze key and is signed `XIII`. The compressed italic hand, uneven ink edge, and seeded angle keep it visibly distinct from the typeset excerpt; the basement key and unlock remain idempotent and exposed in diagnostics.
- Trying the locked Kitchen basement door now gives only generic locked feedback; it no longer repeats the book or reveals the hedge-maze key solution.
- Every ordinary shelf volume in the main Library, upper Reading Room, and basement Archive is now readable and subtly titled in shadow-free matte-black ink on its actual 3D spine. Twenty original titled excerpts shuffle in complete 20-book cycles across 384 physical copies on every fresh run, while the XIII-marked Contestant 13 volume retains its dedicated clue interaction.
- The parchment reader exposes the aimed-at title in the E/touch prompt, manages focus and Escape/E/backdrop closing, and fits desktop and phone layouts. The original books remain instanced; their 384 title planes are grouped into 20 reusable title batches, while 35 invisible shelf-row hit targets resolve individual spines without per-copy meshes, materials, textures, ray targets, or shader lights.
- The mansion now uses one 38-asset CC0 mix behind the existing ♪/M master control: the original roof wash is layered with long concrete-rain detail, lightning selects among four recorded thunder rolls, and grounded distance-driven walking randomizes five wood, stone, or grass steps per surface without firing while stationary, blocked, hidden, paused, or teleported.
- Doors, cabinets, light switches, readable books, hiding, physical pickups/locks, and Workroom keypad feedback now use restrained recorded foley with procedural fallbacks. Runtime diagnostics expose context/asset readiness, cue counts, rain layers, thunder state, and footstep surface/cadence; provenance is recorded in `assets/Sounds/mr-feast/LICENSES.md` and playback has no third-party runtime dependency.
- All 19 framed wall portraits can be tilted and straightened, all 16 standard chairs can be pulled diagonally with their Rapier colliders following the mesh exactly, and the kitchen refrigerator left open counts as a third disorder. A tamper is noticed after `4.2s`, sending Mr. Feast along the authored response graph to correct it: he faces the object through a `2.8s` fixing pause, restores it, chains any queued fixes, then resumes his interrupted patrol point. Camera alarms always preempt and re-queue the errand, self-fixing cancels it, an object he is actively correcting cannot be re-tampered, and a just-fixed object carries a `6s` re-notice cooldown.
- Noticing, fixing, and direct conversation each show one bubble-style line over Mr. Feast's head from per-kind pools that mix irritation, menace, and humor; portrait lines cite the artwork's real painted title. The DOM bubble anchors to his head bone at a fixed screen-space font size (16px desktop / 14px phone), clamps to the stage edges when he is off-screen or behind the camera so a hiding player still sees the reaction, and sizes against the stage rather than the window so embedded and narrow layouts hold.
- A `Speak with Mr. Feast` interaction on the host body and model plays sophisticated, cryptic, occasionally creepy smalltalk from a 14-line pool with no immediate repeats, briefly pauses his patrol walk, and switches to a curt busy pool while he is mid-errand or mid-alarm. In QA mode, close-up face aims still expose the expression-cycle prompt because the talk hitbox deliberately stops below his face.
- Three additional fictional contestants now occupy distinct rooms: Mara Voss, “The Strategist,” in the Library; Kip Solano, “The Wild Card,” in the Ballroom; and Juniper Cross, “The Folklorist,” in the upper Reading Room. Each has a ten-line private dialogue pool with no shared lines or immediate repeats, a named `Speak with …` E/touch prompt, and a speaker-labelled bubble anchored over their animated head.
- The contestant cast is authored through Meshy 6 and auto-rigging, then grounded and reduced in Blender 4.5 to three standard uncompressed GLBs totaling 101,998 triangles and 6,969,740 bytes with 24 bones apiece. Mara and Kip retain 512 px atlases; Juniper uses a 50,000-triangle / 1024 px close-conversation budget with welded smooth normals, zero skin/cloth metalness, and a restrained albedo-emission lift. The active animation layer remains stationary and rotation-only; runtime validation proves all 24 rotation tracks bind and the strongest sampled breathing bone actually changes pose.
- Contestants load asynchronously without blocking mansion startup, use separate mixers, simple fixed colliders and body-only hit targets, cast no dynamic shadows, and report per-character load/rig/animation/placement/dialogue state through `render_game_to_text()`. Continuous patrol-segment clearance is at least 1.8m for the three placements, and partial asset failure is isolated and reported without disabling the other contestants.
- Twenty-five reusable chair/sofa slots now provide exclusive player/NPC reservations. E/touch enters a visible, movement-locked seated state with free look, menus, and danger still active; standing, teleporting, or loading releases transient occupancy, and chair tampering remains available only while its seat is vacant.
- Mara, Kip, and Juniper use sit-dominant ping-pong routines with authored 60/72/84-second seated dwells under a shared 240-second hard ceiling, brief room hangouts, smooth 0.82-second sit/stand interpolation, moving kinematic colliders, and interruption-safe route targets. Every completed stand now requires arrival at a distinct non-seat waypoint at least 0.35m away before reseating, including the former return-leg edge case that paused at the duplicated seat exit and immediately sat again.
- One coherent shoulder/arm/forearm/hand layer now replaces the previous hands-up/shrug silhouette in every contestant activity. Standing hands hang separately, and the rejected source walk-arm tracks are replaced by a bounded opposing `0.105rad` counter-swing whose signed palm normals are reoriented toward the torso every frame. Seated wrist targets are constructed in each thigh's own direction/normal frame rather than lifted in world Y, preserving an actual `0.269–0.277` hip-to-knee fraction; signed fingers follow the thigh and palms lie flat against it. Per-rig normal clearances plus a QA-only deformed skinned-mesh probe prevent both penetration and hovering, and the incoming walk-arm pose is retained through the eased sit blend to remove the prior wrist snap. Seated actions remain stopped so that standing pose cannot leak in; contestant-specific procedural breathing, weight shift, glance, shoulder, and hand motion affects only the upper body while the root and lower body remain planted. Juniper's sofa root is shifted 0.18m toward the cushion front, while her Reading Room sofa alone uses a floor-anchored `0.77` vertical profile with a roughly `0.55m` cushion top and matching Rapier collider. That leaves supported hips, visibly clears the thigh line above the cushion edge, and keeps her root and boots at the original floor height; every other sofa remains unchanged.
- Contestant attention is limited to 3.8m and a roughly 120-degree forward field, split across neck and head, smoothly acquired/released, and hard-clamped near 31.5 degrees yaw / 13.8 degrees pitch. Mr. Feast uses a narrower 4.8m forward-vision gate with occlusion/hiding checks and roughly 28.7-degree / 10.3-degree limits. Deterministic QA proves body yaw never changes, rear/far targets are ignored, ten-second holds stay bounded, and all applied head quaternions remain normalized.
- Every standard chair now shares a floor-anchored `0.86` visual height profile, lowering its cushion top from `0.61m` to about `0.525m` while preserving the existing floor-anchored Rapier/tamper collider. The shortened `1.118m` back posts remain within `0.018m` of the collider top; Mara and Kip retain at least `0.025m` thigh clearance, supported hips, forward knees, floor-near boots, and unchanged floor roots, while all sofa measurements remain independent.
- Holding `Shift` now sprints at a faster named speed while draining a visible energy reserve; exhaustion blocks sprint until the reserve recovers.
- `C` toggles a slower, lower crouched stance with authoritative `0.5` visibility and `0.32` movement-noise multipliers for the future detection system.
- `Tab` opens and closes the combined carried-object and clue dossier; the previous `I` / `J` bindings are retired, while `Escape` opens a true pause menu with Resume, Maximize, Save, Load, and reversible Dev Mode controls.
- A fresh playthrough withholds the left-side Contestant 13 case file and its Library direction until the player discovers the shelf book; later objectives still appear after that first clue.
- The Tab dossier now presents the shovel, B-13 key, badge, and tape as distinct illustrated cards, while recovered clues are written on one cream ruled-paper evidence pad with a mobile-safe scrolling layout.
- Explicit saves preserve the player transform and Contestant 13 state through `RBGameSaves`. Dev Mode grants every current item/clue and testing gate without completing sabotage, cannot be saved, and restores the exact pre-dev snapshot when disabled.
- The west-lawn garden paving remains one continuous `ShapeGeometry` network, with both approach lamps offset onto the grass beyond the walking line.
- The Music Room grand piano now sits `0.45m` farther north from the south wall, while its rotated table and couch use their true oriented footprints instead of oversized axis-aligned collision boxes.
- Mr. Feast skips the basement patrol branch until the story door is unlocked; full-route QA temporarily releases and restores that lock without advancing story state.
- Thirty-two unsynchronized surveillance cameras now scan 24 eligible interior zones and eight outdoor chokepoints. Bathrooms, the coat-closet hiding space, walk-in wardrobes, the Workroom, and the deep maze remain deliberate blind spots.
- Indoor cameras are centered on cardinal walls with their brackets raised flush to each floor's ceiling underside, and their one-way sweeps now take 10.5–14.5 seconds. The Reading Room unit faces into the room, the redundant camera behind the grand-stair mid-landing is gone, and the pool camera is grounded on a dedicated support post. A small readable green LED marks normal scanning; any visible player acquisition gives three red pulses over 2.4 seconds, then turns solid red and follows room movement. A small transient pill says `Spotted` during those pulses and `Being recorded` after lock, then disappears when observation ends. Permitted show filming can visibly track without raising suspicion, while hostile policy keeps a further two-second escape window before the alarm threshold.
- Camera policy progresses from public-show filming to restricted-basement rules and global lockdown. Watched sabotage alarms immediately; blind patron-feed sabotage starts lockdown without summoning Mr. Feast until a later camera sighting.
- Hostile camera exposure has a grace window, consumes the crouch-scaled stealth effective visibility (which multiplies the authoritative `0.5` crouch multiplier underneath), respects active hiding, and uses explicit wall/live-door/hedge occluders. A continuous sighting raises only one latched alarm until line of sight is broken.
- Crouching (`C`) now surfaces a 0–100 stealth meter above the energy HUD, visible only while crouched: an additive exposure mix of stance, torso-height light sampled from the live circuit/auxiliary emitters (point radius windows plus spot cones with penumbra, matching the renderer's punctual falloff), and smoothed motion activity. Crouch-walking clearly drops the score and stillness recovers it; darkness raises it toward `~94–96` over an `0.05` ambient floor; the label reads `Stealth · dark`/`Stealth · lit` and flips to an exposed treatment below `35`. Entering a hiding spot pegs the score at `100` and yields the HUD to the existing hidden pill; the meter stays hidden on phones while crouch remains desktop-only.
- The same model feeds the AI as `state.stealth.effectiveVisibility`: darkness (up to `0.55`) and stillness (up to `0.25`) bonuses multiply the authored `0.5` crouch multiplier only while crouched — a lit moving crouch equals `0.5` exactly, standing stays exactly `1`, and a `0.12` visibility floor plus the `2.6m` sight floor keep point-blank wrongdoing always witnessed. Mr. Feast's witnessed-sight range collapses from the authored `9m` to about `3.7m` against a dark motionless crouch (proven by a staged `4.8m` Music Room infraction that is seen standing lit, unseen dark-crouched, and seen point-blank), which also drains the pursuit give-up clock sooner once a fleeing player goes dark and still. Bonuses only ever make the runner harder to detect, so existing camera/pursuit tuning and regression timing cannot drift.
- Camera alarms record source and last-seen data, then divert Mr. Feast through responding, searching, returning, and patrol along authored route/door points without teleporting. This is bounded investigation, not direct perception or chase.
- The Archive-side service-stair landing now carries an easy-to-find emergency flashlight. Its real E/touch pickup enters the existing Bag exactly once, possession persists, F and the touch `Light` control share one modal-safe toggle, and loading extinguishes all active light/alert state without returning the prop.
- The carried beam uses one warm, narrow, shadow-free SpotLight with no visible held model and a fixed six-spot/eleven-point renderer shape. Its center materially reveals Archive shelving while the edge stays dark; off state retains zero energy without background raycasts. A dark still crouch measures `96.0` concealment off and `71.5` on, while standing remains exactly visibility `1`.
- Switching the flashlight on creates at most one clear-camera `flashlight-use` record per activation, identifies the source/last-seen position, pulses `Flashlight spotted`, and calls the existing bounded Mr. Feast camera investigation. Occluded/camera-free use invents no source, while the ongoing visibility penalty still makes ordinary camera recording and witnessed basement trespass easier.
- The former Workshop and Cold Room are now one continuous, deliberately camera-free `WORKROOM`. The west entrance is the only remaining door; the former east doorway is solid wall and collision, while the two ceiling fixtures, player label, and Mr. Feast route share the merged identity.
- The retained entrance begins behind a persistent four-digit PIN pad with a real in-world terminal and focus-safe desktop/touch overlay. Number entry accepts physical and character-only keyboard events as well as click/touch controls. Explicit save/load and reversible Dev Mode preserve the gate correctly without revealing the code in ordinary diagnostics.
- An eight-screen 4×2 monitor bank cycles through all 32 remaining public cameras. Each low-resolution feed copies its source camera's live lens/yaw/pitch, uses a legible green low-light pass, refreshes one target at a time, and freezes when the player leaves the Workroom vicinity.
- The Workroom now includes a broad operator console, three rolling chairs, three server racks, patch ports, cable trays/bundles, equipment cases, radio, coffee cup, shift logs, binders, and the existing tool bench/relay while preserving the central aisle. Both original ceiling emitters are explicitly reserved inside the unchanged fixed point-light budget; the east fixture remains stronger at `1.55×` intensity with a `7.6m` reach, and three switched emissive task practicals keep the server side readable without darkening the west operator/monitor half.
- Three Meshy-authored, Blender-prepared sculptures now establish the estate's creepy-elegant art language: The Listening Host and The Veiled Waltz replace the foyer's procedural busts, while The Weeping Crown replaces only the primitive fountain figure and preserves the existing basin, water, jets, practical glow, paths, and collider.
- The static Three.js r128 statue assets total 45,985 triangles and 13.1 MB across three uncompressed GLBs with 1024 px PBR textures. Runtime bounds fitting grounds each model at its authored anchor, the foyer pair remains outside the central rug, all three placements have simple fixed colliders, and the black-marble host uses a restrained material-only readability lift rather than a new light.
- Estate-statue diagnostics expose per-model load state, dimensions, triangle/material/texture cost, placement, grounding, and collider state. The provenance manifest preserves the Meshy task IDs/prompts and is regression-locked to the JavaScript runtime IDs and filenames.
- Whole-home patrol still traverses more than 220 authored points across all 29 current major room/stair zones and 20 route doors; while the Workroom is locked, Mr. Feast skips that room branch instead of stalling or walking through its door.
- Mr. Feast again uses the original 65,000-triangle rigged runtime model. The rejected face-retopology model, reports, and Blender scripts are not part of the active manifest or release baseline; the page/runtime and NPC asset keys now share `20260721-stable-face-1` so cached clients cannot retain the rejected selection.
- The 2.01m fit, 24-bone rig, and whole-home animations remain controlled through the existing static Three.js r128 runtime; a replacement facial direction is deferred.
- Mr. Feast's stalk now preserves the source pelvis and full lower-body rotation chain instead of pulling it toward the idle pose. In-browser toe travel is about 3 degrees off-forward rather than 31–36 degrees, and fixed-step planted intervals stay below 0.051m horizontal drift.
- The stalk cadence is calibrated to the existing 0.62m/s patrol at 0.37 playback and scales to about 0.645 for the existing 1.08m/s camera response. Patrol and response movement share a 0.985 facing gate so he pivots before translating through corners.
- The marble path directly inside the large upper foyer window is now a finished `1.7m`-deep gallery with `1.5m` of usable clearance, replacing the exposed `0.9m` strip. A mansion-matched `0.98m` railing, repeated balusters, brass-topped corner newels, and a full Rapier guard join both side-balcony rails across its open edge.
- Mr. Feast's east, center, and west window-gallery patrol points now share the widened deck centerline at `z=11.15`. The player can complete the full upper-balcony loop without falling, and a forward edge probe stops against the new guard while remaining grounded on the second floor.
- Super Slop Brothers now uses six Meshy-authored, 24-bone fighter rigs prepared in Blender 4.5: Rainbot, Gigachad, Mr. Feast, Skibidi, Sigma, and AI Slop Bot. The preserved processed GLBs are 7.39–8.77 MiB each; raw Meshy masters/actions and editable Blender files remain ignored.
- Each fighter has one 8-column × 13-row WebP atlas covering idle, run, jump, fall, hit, shield, dodge, grab, normal attack, and all four directional specials. The six atlases total 2,206,564 bytes, retain at least a six-pixel transparent margin, and contain zero clipped poses.
- The Canvas2D simulation remains authoritative. Root translation is removed from the offline motion bake, active-match sheets load lazily with the old body atlas as fallback, hitlag freezes animation time, left-facing art mirrors without collision changes, and instant gameplay effects start on their rendered release pose.
- All 24 directional specials now carry explicit animation identity. Deterministic QA executes the real dispatcher and verifies each move's projectile, melee, recovery, counter, reflect, teleport/clone, falling object, trap, or summoned dog alongside clip selection and frame progression.
- Super Slop's phone setup surface now expands enough to expose the fighter, mode, stage, and match controls without a tiny nested viewport. The page HUD is one compact row, keyboard-only instructions are hidden on phone layouts, and menu-only screens no longer show combat controls.
- Entering Max reparents the active nine-button touch dock into the fullscreen canvas subtree, applies safe-area-aware 44 px-or-larger targets, leaves the arena center clear in portrait and landscape, and restores the dock and unchanged match state on exit. Native fullscreen and the CSS fallback share the same behavior.
- Reload starts fresh unless the player explicitly restores a saved game. Mr. Feast still has no collider, no attack or damage behavior, and no continuous player perception; outside the bounded camera-alarm investigation his only aggression is the infraction-triggered pursuit below.
- Tilting a portrait or pulling a chair while Mr. Feast has clear line of sight (9m range, forward cone, wall/door occlusion respected), or while the HUD reads `Being recorded`, starts a pursuit at `1.95m/s` — hard-capped below the `2.2m/s` walk speed so walking away always escapes while crouching does not. Personal sight or hostile recording refreshes one last-known position; after both are broken he follows only that stale clue, gives up after `8s` unseen or `3.4s` hidden, and can never catch a hidden player. Ordinary fridge opening and straightening objects never trigger pursuit, though self-fixing the evidence does not call off a pursuit already started.
- A catch on the main or second floor delivers a spoken warning while he faces the player, then he resumes patrol; warnings and catches are counted in diagnostics. A catch anywhere in the basement is game over: a modal `CAUGHT` overlay freezes the simulation and input, releases pointer lock, and offers `Load last save` and `Start over`. Loading is treated as time travel — it clears the fail state and any pursuit/errand/alarm and rejoins Mr. Feast to his nearest patrol waypoint without teleporting.
- Pursuit outranks housekeeping errands and camera alarms (a new alarm cannot reroute an active chase), uses the run animation at a rate scaled to the capped speed, and speaks from dedicated witnessed/recorded/warning/lost/basement line pools through the existing overhead bubble.
- Fixed a reported bug where a warning or give-up sweep was silently hijacked, within about a second, by that same tampered object's own overdue housekeeping notice — making it look like Mr. Feast simply walked over, fixed the object, and left, with no perceptible chase or warning. A catch that resolves as a warning now resolves the exact tampered object as part of the catch itself (no separate later visit needed), and a `pursuit.cooldownActive` flag blocks housekeeping dispatch for the whole post-pursuit searching/returning sequence either way; an escaped (given-up) tamper still gets a normal, undisturbed housekeeping visit once he settles back into patrol.
- Presence in the basement is now itself the offense: while the player's feet are below the basement threshold and not hidden, being personally seen (sight cone plus occlusion ray) or hostile-recorded for a `0.55s` dwell starts a pursuit of kind `trespass` with its own line pool — no tamper required. All pursuit rules apply, so a basement catch is game over while an escape upstairs downgrades to a warning; this fixes the report that basement captures never fired, since no tamperable object exists below the main floor.
- Chase pathing overhaul: pursuit/errand node targeting is floor-plane-biased (a witnessed basement player cannot strand him in the room directly overhead, and a wall-hung portrait targets floor nodes instead of stair landings), and the pursuit graph lazily adds straight-line shortcut edges between same-floor nodes whose segment crosses no wall/hedge box and clears every hinged-door leaf by `1.0m` (door crossings stay on authored door-aware edges). While Mr. Feast has genuine personal sight, a clear same-floor target inside `9m` bypasses the graph for direct collision-aware steering; walls, doors, stairs, loss of sight, or a physical stall fall back to the graph and its last-known target. The unseen clock now drains regardless of progress, and a `0.7s` stall watchdog still blacklists a blocked edge for `20s` before rerouting. Pursuit, errands, and their returns use shortcuts; the bounded camera-alarm investigation keeps its fully authored route.

## Important constraint

The Workroom keypad and security hub are implemented in Milestone 37, and Milestone 49 adds the in-world clue that reveals its combination (the Painting Room scratch hunt assembling `0513`). The broader basement sabotage network beyond the patron feed remains deferred.

Mobile sprint/crouch buttons remain deferred; Milestone 35 adds the desktop keyboard controls while preserving the existing touch exploration and interaction layer.

Continuous Mr. Feast sight/hearing perception and free-roaming chase remain deferred: Milestone 46 adds only infraction-triggered pursuit (witnessed or on-camera tampering), a main/upper-floor warning, and the basement capture fail state. The renovation contract's capture-boundary pin now enforces that failure exists solely through that speed-capped path. Milestone 51 only narrows the existing witnessed-sight and camera checks against a crouched runner. Milestone 56 adds one discrete, clear-camera flashlight activation event plus a while-on visibility cost; it does not add hearing, omniscient camera sources, direct pursuit, or another continuous Mr. Feast perception system. Milestone 57 makes that bounded pursuit non-omniscient through sight/camera-owned last-known memory and adds animation-synced host footstep audio only; `stealthNoiseMultiplier` remains audio-only.

The current face-retopology experiment is paused and must not be published or treated as the active runtime. A future facial pass needs a new visual direction before implementation resumes.

## Verification

- 2026-07-22 pursuit evasion/pathing/footsteps: red-first focused and renovation checks failed on all seven missing contracts before implementation. Final `node --check assets/js/mr-feast-mansion.js`, `test-mr-feast-renovation.mjs`, hardware-Chrome `test-mr-feast-pursuit-evasion.mjs`, `test-mr-feast-caught-pursuit.mjs`, `test-mr-feast-camera-security.mjs`, `test-mr-feast-grounded-gait.mjs`, `test-mr-feast-audio-upgrade.mjs`, the full desktop/mobile `test-mr-feast-contestant-13.mjs`, and `git diff --check` pass. The focused run records four stalk and five run contacts at the sampled phases, 102 clear-line direct-steering frames, zero teleports, an unchanged last-known Music Room target after unseen movement, and a real coat-closet escape ending `lost`. Proof is under `output/playwright/mr-feast-pursuit-evasion/`.
- 2026-07-22 point-blank basement awareness follow-up: live Chromium reproduced a `1.9m` rear approach leaving pursuit inactive and trespass dwell at zero. A red-first regression now proves close clear basement awareness starts pursuit with a `proximity` tracking source, while hiding, the Archive corridor wall, and main-floor separation all block it. Syntax, renovation, focused pursuit/evasion, caught-pursuit, camera-security, stealth-meter, audio-upgrade, full desktop/mobile Contestant 13, and `git diff --check` pass. Visual proof is `output/playwright/mr-feast-pursuit-evasion/close-basement-awareness.png`.
- 2026-07-22 cross-floor basement chase follow-up: live Chromium proved the boiler camera started a valid 15-node pursuit from the Kitchen route while the physical basement door was unlocked, but each `0.9s` repath re-anchored Mr. Feast toward the upper landing; after 20 simulated seconds he remained near the stair top. The red-first regression now proves the hostile camera route carries him below `y=-3.7` in 10 seconds by retaining active stair/ramp legs, after which ordinary last-known replanning resumes. Syntax, focused pursuit/evasion, caught-pursuit, camera-security, stealth-meter, renovation, full desktop/mobile Contestant 13, and `git diff --check` pass. Visual proof is `output/playwright/mr-feast-pursuit-evasion/basement-cross-floor-chase.png`.
- 2026-07-22 basement flashlight: red-first focused/static checks failed before implementation, then `node --check assets/js/mr-feast-mansion.js`, all changed test syntax checks, `test-mr-feast-renovation.mjs`, `test-mr-feast-flashlight.mjs`, `test-mr-feast-contestant-13.mjs`, `test-mr-feast-stealth-meter.mjs`, `test-mr-feast-camera-security.mjs`, and `git diff --check` pass. The light-only/brighter follow-up also failed red on the old carried geometry and `68` tuning, then passed in a hardware-accelerated browser with the held model absent and authored intensity `74`; Sharp measures `+7.1` center versus effectively `+0.0` edge luminance. The focused browser suite proves a real landing pickup, fresh-run F/touch gating, one Bag item, save/load-off restoration, unchanged `{spot:6, point:11}` topology, cooldown-cleared camera occlusion, one recoverable Mr. Feast response, stealth `96.0 → 71.5`, and 390×844 controls with zero errors. Proof is under `output/playwright/mr-feast-flashlight/`. Adjacent Player Systems still stops at its documented headless Max click, Caught/Pursuit at its unrelated later-housekeeping expectation, and Basement Key Trail at its existing post-Feast `digSiteExcavated` wait; each progressed beyond the stabilized capture/startup boundary and cleaned up normally.
- 2026-07-21 face-release repair: the reported bulged eyes and facial breaks reproduce in the neutral Blender preview before Three.js loads. Geometry audit found a `193.9mm` replacement-face depth span, `120.8mm` maximum adjacent depth jump, 926 edges over `10mm`, overlapping retained/replacement surfaces, and protruding separate eye spheres; hiding/recoloring/recessing individual parts in the deployed browser did not repair the underlying surface. The active manifest now selects `processed/mr-feast-game-rigged.glb` (65,000 triangles, one skinned mesh, 24 bones), the unsupported QA expression prompt is gated off, and both cache identities are `20260721-stable-face-1`. A red-first release gate failed on the shipped manifest, then passed after the rollback. `node --check assets/js/mr-feast-mansion.js`, both focused test-script syntax checks, `node scripts/test-mr-feast-renovation.mjs`, the complete desktop/mobile `node scripts/test-mr-feast-contestant-13.mjs`, and `git diff --check` pass. Browser diagnostics prove zero `MrFeast_RetopoFace`/`MrFeast_Eye` scene objects, stable 2.01m/24-bone locomotion, a real `Speak with Mr. Feast` prompt instead of a no-op expression control, full story/mobile completion, and zero unexpected console errors. Visual proof is `output/playwright/mr-feast-contestant-13/mr-feast-stable-face-desktop.png`; the deployed broken-asset audit remains under `output/iterate/mr-feast-face-fix-audit/`.
- 2026-07-21 competition-staging follow-up: `node --check assets/js/mr-feast-mansion.js`, both focused test-script syntax checks, the full `test-mr-feast-feast-says.mjs` and `test-mr-feast-storm-run.mjs` browser suites, `test-mr-feast-renovation.mjs`, `test-mr-feast-contestant-13.mjs`, and `git diff --check` pass. Feast Says diagnostics prove the long-lens cinema camera components, fixed lineup target, three-quarter aim, and two player-facing fixture heads stay visible and static through briefing, commands, and results, with zero lineup rings and zero action pads; fresh proof is `feast-says-long-lens-player-lights-{desktop,mobile}.png`. Storm Run diagnostics prove the waiting host remains `3m` clear of the back door, the briefing continuously reapplies the face-to-face pose, and the temporary player-side key returns to the regular storm direction at `Run!`; fresh proof is `storm-run-bright-intro-{desktop,mobile}.png`.
- 2026-07-21 Storm Run final-straight correction: the user's in-game reference clarified that the prior rear-opening interpretation was too close. The red-first focused regression failed on the old `(22,-13.75) → (20.5,-13.75)` pair. The authored pair is now the south-turn cell `(22,-0.25)` to the far-end cell `(22,-13.75)`, immediately before the westward exit. Live Chromium diagnostics measure a clear centered `13.5m` sightline, `0.2109` projected human-scale height, `1.0` host-facing dot, zero console errors, rear-exit intensity `0` through the flash, and restored active energy after Mr. Feast disappears. Focused proof is `output/iterate/2026-07-21-storm-run-final-straight-lightning.png`.

- `node --check assets/js/mr-feast-mansion.js`, `node --check scripts/test-mr-feast-storm-run.mjs`, `node scripts/test-mr-feast-storm-run.mjs`, and `git diff --check` — pass the red-first Storm Run refinements: pre-Feast lock, exact 599.9/600-second boundary, menu pause, next-clue preservation, shared investigation hold, active-pursuit suspension, visible back-door rule briefing without `Beat Mara`, spoken `3–2–1` and plain `Run!`, no checkpoint speech after release, west-facing release, live sprint/energy feedback, twelve visually discoverable ordered breadcrumbs with collection chimes and a collision-clear route, stamina-fair animated runners, exactly three mapped deep-shadow reveals, delayed first activation at the north garden/front-yard edge, delayed second activation at the front-door crossing, human model scale on all three, separate distant lightning fill, persistent route-window arming, away/down-facing silence and retained one-shots, clear-ray/actual-camera-view activation, desktop/phone readability, visible runners, stronger local and global lightning illumination, a dedicated scare sting, recorded-only trimmed `2.1x` thunder with zero procedural crack layers, roughly `16dB` rain duck, trusted-gesture replay after mobile audio interruption, darkness-synced disappearance under `1.2s`, zero hazard penalty, save normalization, stable Mr. Feast back-door visibility, distance-plus-occlusion aftermath cleanup, Mara's hands-over-face pose, desktop/phone geometry, and zero console errors. The 390×844 headless first-scare profile holds renderer programs at `54 → 54` and reduces the isolated prior-build stall from `5990.8ms` to `324.6ms` under SwiftShader; the relative comparison diagnoses the removed compile spike, while absolute timings are not hardware-phone frame rates.
- The focused outcome pass now additionally proves the witnessed finish: Mara stays visible, unavailable, and slumped for Mr. Feast's result and her own sad line; Juniper walks the full pool-to-Reading-Room route; Mr. Feast remains visible on his exact mark while the nearby player enters the rear lounge; only a later sixteen-metre-plus occluded break cleans up from the Reading Room; and a save loaded while Juniper is walking clears her temporary route and closes the terrace door. The full current-worktree Feast Says regression also passes after the shared aftermath generalization.
- A trusted-click WebAudio browser probe started the real Storm Run race in a running audio context, observed the three countdown beeps plus GO and a checkpoint chime, then suspended the context before the northwest-tree reveal. The close strike remained queued while suspended and, on the next real key gesture, produced exactly one `storm-run` close-strike cue and one recorded thunder roll at `0.02s` delay and `2.1x` volume with zero browser errors.
- `node scripts/test-mr-feast-audio-upgrade.mjs` still stops at its pre-existing stale source-shape assertion for `const baseVolume`; both HEAD and this working tree use the same explicit `let stepVolume` crouch/walk/sprint branches, and the complete `updateFootsteps()` function remains byte-identical (`sha256 8b6d0952ef2a62e22b4208ab47d384dd9863b02361b4f0bd31b4e1cf4e57321c`). The Storm Run suite's production-graph loudness and interrupted-context checks cover the changed audio path directly.
- Post-Storm adjacent regressions passed the full `test-mr-feast-feast-says.mjs`, `test-mr-feast-renovation.mjs`, `test-mr-feast-player-systems.mjs`, `test-mr-feast-workroom-code-clue.mjs`, `test-mr-feast-basement-key-trail.mjs`, `test-mr-feast-contestant-13.mjs`, `test-mr-feast-contestant-conversations.mjs`, and `test-mr-feast-seating-and-routes.mjs` suites. The story suites now assert the first post-Feast clue calls Game 2, remains earned, and releases the trail only after a player win.
- Storm Run captures — `mr-feast-{northwest-tree-line,northeast-tree-line,maze-turn}-lightning-{desktop,mobile}.png` prove the three mapped human-scale reveals at 1280×820 and 390×844, while `hedge-maze-north-mobile.png` continues to prove the readable north-maze path. `mara-back-door-elimination.png`, `mara-back-door-cover-face.png`, and `mara-back-door-cover-face-mobile.png` prove the rear-door ending, speech framing, both-hands grief pose, and Juniper's visible departure.
- The latest Storm Run visual pass moves the first two lightning silhouettes beside actual tree trunks (`1.73m` and `1.71m` offsets) while keeping their clear torso rays, human-scale projections, deep-shadow exposure, facing gates, desktop/390×844 readability, and zero browser errors. The refreshed northwest/northeast desktop and mobile captures live under `output/playwright/mr-feast-storm-run/`.
- `node --check assets/js/mr-feast-mansion.js`, `node --check scripts/test-mr-feast-feast-says.mjs`, `node scripts/test-mr-feast-feast-says.mjs`, and `git diff --check` cover the final Feast Says refinement: authoritative spoken rules/commands/verdicts, the exact six-call order, two plain fake-outs correctly scored by stillness on desktop/touch, 7.5/7.2-second pacing, look-and-Interact pointing, world-displacement sacrifice, distinct NPC targets, stable planted response posing, contestant banter and host warnings, the Kip aftermath, save/loss/partial-cast fallback, minimal responsive strips, and zero unexpected console errors.
- The production-call refinement passes runtime/test syntax, the full Feast Says and Storm Run browser suites, renovation invariants, the full desktop/mobile Contestant 13 suite, and `git diff --check`. The focused regressions now prove both signless camera/two-light/boom sets, host-centered E/touch starts, exact five-minute called-state boundaries, blocking-UI and character-load fairness, precise called-save restoration, fresh five-minute transient-save normalization, waiting-host restaging, and recoverable no-show eliminations. Fresh desktop and 390×844 captures in `output/iterate/competition-call-{feast,storm}-{desktop,mobile}.png` confirm the production staging and mobile-safe prompt/control spacing with no browser errors.
- The 2026-07-21 staging polish rebuilds the shared camera as a `long-lens-cinema` pedestal with a rear battery, matte box, twin rails, viewfinder, monitor, pan handle, and long rear-weighted body. Feast Says adds the event-local `~0.75m` lineup setback and static lineup-facing camera/lamp diagnostics; Storm Run preserves the roomier shared exterior placement. The waiting host remains exactly `3m` south of the actual back-door plane at yaw `0`, then pivots to the player for briefing.
- Final verification for that staging polish passes runtime and focused-test syntax, the complete Feast Says suite, the complete Storm Run suite, renovation invariants, the full desktop/mobile Contestant 13 progression suite, and `git diff --check`. Playwright confirms the two Feast Says lamps and long-lens rig remain static on the lineup, while Storm Run reports the player-side `storm-run-briefing` moon pose through the countdown and a restored `night` pose at release; no game/runtime errors appeared.
- Post-refinement adjacent reruns passed `test-mr-feast-renovation.mjs` and the full `test-mr-feast-storm-run.mjs`, preserving static mansion invariants and the post-aftermath Game 2 timer/clue/staging/race handoff.
- Current adjacent passes: renovation, Storm Run, contestant conversations, seating/routes, workroom-code, basement-key, and full desktop/mobile Contestant 13. The story regressions now finish each short competition result card before asserting the investigation HUD handoff. The player-systems attempt still stopped at its existing fullscreen/maximize wait; a separate opening-welcome attempt timed out waiting for the optional host model loaded flag, with no Feast Says assertion or browser error.
- Refreshed Feast Says captures — `output/playwright/mr-feast-feast-says/six-command-round-desktop.png` and the contestant response captures show the minimal strip plus Mr. Feast's spoken command; `result-banter-point-desktop.png` and `result-warning-point-desktop.png` prove the verdict/banter/warning handoff; the landscape captures cover touch-safe speech-led play.
- `node --check assets/js/mr-feast-mansion.js` — passed
- `node scripts/test-mr-feast-menu-viewport.mjs` — passed a real saved-game intro and the Escape menu at 1024×600, 1280×720, 390×844, 390×667, 560×600, 844×390, and 568×320 at DPR 2; every panel and 44 px-or-larger action stayed inside the visible stage/browser with no overflow, Start kept a stable 366 px desktop stage, a 600→720→600 viewport cycle returned exactly to its original stage/canvas height, opening the menu did not move page scroll, and no unexpected browser errors appeared
- `node --check scripts/test-mr-feast-stealth-meter.mjs` and `node scripts/test-mr-feast-stealth-meter.mjs` — passed (red-first before implementation): exact standing `1`/crouched `0.5` stance pins, crouch-only accessible meter HUD with aria mirroring, clear crouch-walk meter drop with stillness recovery, real-circuit lit/dark response with the deterministic QA light override, the staged `4.8m` witnessed/unseen/point-blank sight probe, hiding handoff to the hidden pill, phone-layout hiding with zero overflow, and zero console errors
- Renovation section 55 stealth pins (constants table, HUD markup, circuit sampling, crouch-gated bonuses, sight gating, camera consumption, diagnostics) failed red before implementation and pass green after; the section 51 camera pin now accepts the `state.stealth.effectiveVisibility` consumption that multiplies the Milestone 35 crouch value underneath
- Post-stealth adjacent reruns passed `test-mr-feast-renovation.mjs`, `test-mr-feast-player-systems.mjs` (full pass, including the previously flaky fullscreen checkpoint), `test-mr-feast-camera-security.mjs`, and `test-mr-feast-caught-pursuit.mjs`
- Stealth meter captures — `output/playwright/mr-feast-stealth-meter/stealth-meter-crouch-lit-desktop.png` shows `Stealth · lit 62` above the crouch energy meter in the lit foyer, `stealth-meter-crouch-dark-desktop.png` shows `Stealth · dark 94` with the circuits off, and `stealth-hud-mobile.png` confirms the untouched 390×844 touch HUD
- `node --check scripts/test-mr-feast-load-reliability.mjs` and `node scripts/test-mr-feast-load-reliability.mjs` — passed a delayed cold core-runtime response, separately delayed texture/statue phases that exceed the old absolute QA deadline only in aggregate, retained true-stall Retry recovery, `render_game_to_text()` readiness checks, two 1280×820 captures, and zero browser-console errors
- `node --check scripts/test-mr-feast-opening-welcome.mjs` and `node scripts/test-mr-feast-opening-welcome.mjs` — passed front-door staging and facing, movement/interaction gating, the exact seven-line briefing contract, 6–10 second complete-line dwell times, real wall-clock auto-progression, the guarded E/tap Continue path, clean non-repeating release to exploration, 1280×820 desktop and 390×844 touch layout, and zero browser-console errors
- Post-welcome adjacent reruns passed `test-mr-feast-renovation.mjs`, `test-mr-feast-contestant-13.mjs`, `test-mr-feast-tamper-distractions.mjs`, and `test-mr-feast-contestant-conversations.mjs`; `git diff --check` also passed. The player-systems suite still stops at its known unrelated fullscreen/maximize assertion.
- Front-door welcome captures — `output/playwright/mr-feast-opening-welcome/front-door-welcome-desktop.png` shows Mr. Feast staged in the foyer with the complete speaker-labelled bubble, while `front-door-welcome-mobile.png` shows the final threat and touch-safe Continue prompt at 390×844.
- `node --check scripts/test-mr-feast-basement-key-trail.mjs` — passed
- `node scripts/test-mr-feast-basement-key-trail.mjs` — passed after the shelf-spacing, `XIII`, and clue-free locked-door refinements: real E/touch interactions, reserved book slot diagnostics, dual clue copy, generic early-door feedback, early gates, idempotent maze key, locked/unlocked door state, full-route lock restoration, Archive recording, Workshop sabotage, desktop/mobile layout, and zero console errors
- `node scripts/test-mr-feast-renovation.mjs` — all current renovation invariants passed after the concurrent Milestone 46 contract update; the contestant slice preserves the caught-in-the-act boundary
- `node scripts/test-mr-feast-readable-books.mjs` — passed exactly 20 unique lore titles, all 384 physical assignments and shadow-free matte-black spine labels in 20 reusable batches, Library/Reading Room/Archive distribution, seed-based reshuffling, physical E interaction, seeded XIII print/position variation, separate printed prose and angled hurried marginalia, focus-managed desktop/mobile parchment layout, unchanged shader-light layout, and zero browser errors
- `node scripts/test-mr-feast-audio-upgrade.mjs` — passed trusted-click Web Audio unlock, all 38 declared samples decoded, layered recorded rain plus one-layer recorded fallback with the optional detail track blocked, four recorded thunder variants, grounded wood/stone/grass steps, walk/crouch/sprint cadence separation, stationary/teleport/pause/hide suppression, sampled book/light/door cues, complete ♪/M mute behavior, local CC0 provenance, and zero browser errors
- `node scripts/test-mr-feast-tamper-distractions.mjs` — passed 19/16/1 portrait/chair/fridge registration, real E-key portrait tilt and collider-synced chair pull with straighten prompts, deterministic notice dispatch with an upset line, the full walk-fix-return errand with restored decor and zero teleports through the bounded responding/searching/returning states, self-fix cancellation with re-tamper cooldown, refrigerator closing, camera-alarm preemption with re-queue and later completion, real talk interaction with a complete-line movement pause, live visual head tracking after the player moves, smooth bounded torso follow for a rear player, and alarm/trespass preemption that continues the security response, non-repeating smalltalk, busy pool mid-errand, on-screen and edge-clamped bubble geometry at readable font sizes, and desktop/mobile layout with zero console errors
- `node scripts/test-mr-feast-contestant-conversations.mjs` — passed all three Meshy/Blender asset budgets, exact cast/persona/room contracts, 24-bone rigs, 24 bound stationary rotation tracks with observed bone motion, continuous patrol clearance, real E interaction, ten private non-repeating lines each, named speaker labels, Mr. Feast speech compatibility, desktop/mobile layout, touch input, and zero browser errors
- `node scripts/test-mr-feast-seating-and-routes.mjs` — passed desktop and 390×844 touch sitting, authoritative seated player position, movement lock with retained danger/menu access, exclusive/transient occupancy, chair-tamper compatibility, all three complete walk-idle-sit cycles, the sub-five-minute dwell ceiling, two matched post-seat departures per deterministic cycle, planted procedural seated motion for all three contestants, upper-thigh supported arms with `45–105°` elbow flexion, `0.24–0.36` longitudinal wrist placement, individually signed palm-down/finger-forward axes, flat wrist-to-fingertip planes, real deformed hand-to-thigh surface clearance, inward walking-palm alignment of at least `0.70`, Kip wrist-ring thickness retention around `0.92` with hand offsets below `70°`, and sub-`0.22rad` transition-frame arm motion, isolated `0.77` Reading Room sofa height with matched cushion/support/collider measurements, isolated `0.86` regular-chair height with unchanged tamper colliders, positive thigh clearance and floor-near boots for Juniper, Mara, and Kip with unchanged floor roots, visible `Head → headfront` Mr. Feast tracking with rear/distant neutral and anatomical clamps, upright 1.65–1.9m vertical-major GLB bounds, fixed-furniture rejection and floor-route steering, patrol clearance, moving-collider alignment, smooth seat ingress/egress, relaxed neutral rest poses, optional-walk failure isolation, moving dialogue anchors, multi-angle chair/sofa visual proof, and zero browser errors
- Seating refinement captures — `output/playwright/mr-feast-seating-and-routes/juniper-seated-lowered-sofa-three-quarter.png` confirms the shorter Reading Room couch sits below Juniper's thigh line; `mara-voss-regular-chair-three-quarter.png` and Kip's `kip-solano-regular-chair-{front,profile,three-quarter}.png` views confirm the standard cushion sits below the contestant thigh line while hips remain supported and boots stay floor-aligned.
- Final contestant-hand captures — `output/iterate/contestant-hand-fix/{mara-voss,kip-solano,juniper-cross}-seated-hands-{front,side}.png` show separated, flat hands over the upper thighs; matching `*-walking-hands-{front,side}.png` views show relaxed arms with the palms' narrow edges outward and palm surfaces facing the body. The fresh Chromium probe reported no console errors, `0.269–0.277` longitudinal wrist placement, positive deformed surface clearance, and `1.0` minimum walking palm-to-body alignment for every contestant.
- Post-hand-pose reruns passed `test-mr-feast-opening-welcome.mjs`, `test-mr-feast-renovation.mjs`, `test-mr-feast-contestant-13.mjs`, `test-mr-feast-contestant-conversations.mjs`, and `test-mr-feast-tamper-distractions.mjs`. The player-systems rerun again stops only at its existing fullscreen/maximize assertion; contestant hand, seating, locomotion, and player-control code all pass before that unrelated checkpoint.
- Post-seating adjacent reruns passed `test-mr-feast-contestant-conversations.mjs`, `test-mr-feast-player-systems.mjs`, `test-mr-feast-tamper-distractions.mjs`, `test-mr-feast-contestant-13.mjs`, and `test-mr-feast-renovation.mjs`.
- Post-orientation correction reruns passed runtime/test syntax, `test-mr-feast-renovation.mjs`, `test-mr-feast-seating-and-routes.mjs`, `test-mr-feast-contestant-conversations.mjs`, `test-mr-feast-tamper-distractions.mjs`, `test-mr-feast-caught-pursuit.mjs`, and the full-route `test-mr-feast-contestant-13.mjs` browser suite.
- The adjacent `test-mr-feast-player-systems.mjs` rerun still stops at its fullscreen-maximize assertion because QA does not report the stage as maximized after the click; that fullscreen path is unchanged by the contestant orientation/collision slice.
- `python3 -m py_compile scripts/blender/prepare-mansion-contestant.py` plus syntax checks for the runtime, Meshy helper, animation extractor, and focused regression — passed
- Post-fix combined `node scripts/test-mr-feast-contestant-13.mjs` rerun — passes again: the Milestone 45 talk hitbox now stops below the face so the `mrFeastFaceClose` QA expression-cycle checkpoint and the new `Speak with Mr. Feast` body prompt coexist
- `node scripts/test-mr-feast-caught-pursuit.mjs` — passed the hard pursuit-speed cap under player walk speed, real E-key witnessed trigger with sight-cone/occlusion negatives, straighten non-trigger, run-animation pursuit with zero teleports, main-floor warning catch with an uninterrupted state trace and the caught object silently resolved with zero later re-dispatch, hidden-player give-up with an equally uninterrupted sweep and a normal separate later fix, live `Being recorded` trigger, basement catch game over with the `CAUGHT` overlay, frozen simulation underneath, working load recovery to the pre-capture save, mobile overlay layout, and zero console errors
- Previous garden browser proof completed both connection routes with zero fall recoveries and confirmed one `18.8m × 32.4m` walkway mesh
- `node scripts/test-mr-feast-contestant-13.mjs` — passes progression, patrol, persistence, accessibility, mobile touch, and discovery-first objective visibility; its experimental facial-checkpoint coverage is paused with Milestone 33
- `node scripts/test-mr-feast-player-systems.mjs` — passed real-browser keyboard, Rapier movement, stamina lifecycle, crouch eye/stealth contract, Tab dossier toggling with retired I/J bindings, withheld opening guidance, focus-safe pause, maximize, save/load, and reversible Dev Mode with zero console errors
- `node scripts/test-mr-feast-camera-security.mjs` — passed exact 32-camera placement, 24 ceiling-height wall-centered indoor mounts, camera-free Workroom, corrected Reading Room facing, removed mid-landing unit, supported pool fixture, 10.5–14.5 second one-way sweep/reversal, rendered green/red fixture-pixel checks, three-pulse warning, permitted and hostile solid-red tracking, `Spotted`/`Being recorded` status transitions, immediate status dismissal after cover, two-second pre-alarm grace, physical blind-side traversal, show/restricted/lockdown policy, watched sabotage, crouch/hiding, natural and deterministic occlusion, alarm latching, Mr. Feast response/search/return, unchanged light layout, and desktop/mobile HUD checks with zero console errors
- `node scripts/test-mr-feast-workroom-security-hub.mjs` — passed merged west/east room identity, removed doorway, physical-code and character-only keyboard entry, real E/click/touch keypad entry, wrong/correct PIN states, explicit save/load persistence, eight independent render targets, full 32-camera roster, camera-free Workroom diagnostics, distinct/nonblank feed signatures, live sweep-image change, one-feed-per-frame budget, renderer restoration, both budget-selected Workroom fixtures, three task practicals, dual-side luminance, ambience diagnostics, and desktop/mobile layout
- `node scripts/test-mr-feast-estate-statues.mjs` — passed three browser-safe GLB budgets, runtime loading, bounds fitting, grounding, three fixed colliders, removal of the old foyer/fountain primitives, central-aisle clearance, deterministic foyer/garden framing, and zero console errors
- `node scripts/test-mr-feast-grounded-gait.mjs` — passed source lower-body preservation, 3-degree toe travel planes, stride-scaled patrol/response cadence, repeated planted-toe drift, aligned corner translation, four side-on gait phases, and zero console errors
- `node scripts/test-mr-feast-upper-window-gallery.mjs` — passed `1.7m` deck and `1.5m` usable-clearance diagnostics, a real forward walk into the physical upper guard, full balcony-loop traversal with zero fall recoveries, upper/main-floor framing, stable circuits, and zero console errors
- `python3 -m py_compile scripts/blender/prepare-estate-statue.py` and `node --check scripts/meshy-generate.mjs` — passed the reusable Meshy preview/refine and Blender static-prop preparation pipeline checks
- Estate statue browser captures — `output/playwright/mr-feast-estate-statues/foyer-statues-desktop.png` and `garden-fountain-statue-desktop.png` confirm the balanced wall-side foyer composition and the retained working fountain at 1280×820
- Grounded gait browser captures — `output/playwright/mr-feast-grounded-gait/grounded-stalk-00.png`, `grounded-stalk-25.png`, `grounded-stalk-50.png`, `grounded-stalk-75.png`, `corner-pivot-start.png`, and `corner-first-planted-steps.png` confirm a forward-aligned side profile plus pivot-before-travel cornering at 1280×820
- Upper window gallery captures — `output/iterate/2026-07-16-upper-window-gallery-after.png` and `output/iterate/2026-07-16-upper-window-gallery-foyer-after.png` confirm the wider marble path, continuous front guard, clean side-rail returns, large-window relationship, and foyer sightline at 1280×820
- Workroom browser captures — `output/playwright/mr-feast-workroom-security-hub/workroom-monitor-wall-desktop.png`, `workroom-both-sides-desktop.png`, `workroom-keypad-desktop.png`, and `workroom-keypad-mobile.png` confirm the camera-free live 4×2 wall, balanced room lighting, and working access terminal at 1280×820 and 390×844. In `output/iterate/2026-07-16-workroom-dual-lighting-before.png` versus `output/iterate/2026-07-16-workroom-dual-lighting-after.png`, the west operator region rises from `0.304` / `0.07%` to `20.367` / `47.21%` while the east server region holds at `52.970` / `62.09%` mean-luminance/visible-pixel coverage.
- Camera browser captures — `output/playwright/mr-feast-camera-security/camera-indicator-green-desktop.png`, `camera-indicator-warning-red-desktop.png`, `camera-permitted-tracking-desktop.png`, `camera-solid-red-tracking-desktop.png`, `camera-status-desktop.png`, and `camera-status-mobile.png` confirm readable fixture feedback, tracking states, and the transient status pill at 1280×820 and 390×844
- Camera placement captures — `output/iterate/2026-07-16-camera-reading-facing.png`, `2026-07-16-camera-stair-landing-removed.png`, and `2026-07-16-camera-pool-post-close.png` confirm the corrected Reading Room facing, empty mid-landing wall, ceiling-height indoor bracket, and grounded pool support at 1280×820 with zero console errors
- The focused player-system suite also verifies four unique scalable object icons, the ruled handwritten clue pad, and in-stage title/close placement without horizontal overflow at 390×844
- `musicTableWestClearance` QA route — clears the couch-table aisle beyond `z=8.0`, grounded with zero fall recoveries, unchanged light circuits, and zero console errors
- Browser captures — `output/iterate/2026-07-15-music-room-layout-a.png` and `output/iterate/2026-07-15-music-room-layout-b.png` confirm the piano wall clearance and corrected center-table layout
- Browser capture — `output/iterate/2026-07-16-tab-inventory-no-library-prompt.png` confirms the opening foyer has no left-side Library direction and displays the new Tab inventory control
- Browser captures — `output/iterate/2026-07-16-inventory-icons-notepad-desktop.png` and `output/iterate/2026-07-16-inventory-icons-notepad-mobile.png` confirm the illustrated object cards and evidence-pad treatment at both target layouts
- `python3 -m py_compile scripts/blender/retopologize-mr-feast-face.py` — passed
- `python3 -m py_compile scripts/blender/prepare-super-slop-fighter.py scripts/build-super-slop-animation-atlas.py` — passed under the bundled Python/Blender 4.5 toolchain
- `node --check assets/js/super-slop-brothers.js` and `node --check scripts/test-super-slop-character-animations.mjs` — passed with bundled Node 24
- `node scripts/test-super-slop-character-animations.mjs` — passed six manifests/rigs/sheets, all 624 decoded poses, exact atlas layout, animation variation, all 24 real special dispatches, release-pose sync, frame progression, hitlag freeze, read-only diagnostics, lazy active-roster loading, forced legacy fallback, four-fighter rendering, desktop/mobile layout, touch controls, and zero unexpected browser errors
- Super Slop browser captures — `output/iterate/super-slop-character-upgrade/all-24-specials-contact-sheet.jpg` summarizes the 24 deterministic ability captures; the source desktop, mobile, and fallback proofs are under `output/playwright/super-slop-character-animations/`
- `node scripts/test-super-slop-mobile-maximize.mjs` — passed 390×844 embedded/portrait-max and 844×390 landscape-max layouts, native fullscreen subtree and rejected-request fallback paths, nine visible non-overlapping touch targets, safe-area/chrome geometry, real movement and attack input, max exit/restoration, zero horizontal overflow, and zero console/page errors
- Super Slop mobile captures — `output/playwright/super-slop-mobile-maximize/after-portrait-embedded.png`, `after-portrait-max.png`, and `after-landscape-max.png` confirm the complete phone setup UI, lower-letterbox portrait dock, and edge-mounted landscape controls
- Browser captures — `output/playwright/mr-feast-player-systems/sprint-energy-hud-desktop.png`, `escape-menu-desktop.png`, and `inventory-and-clues-dev-desktop.png` confirm the new HUD and overlays; the existing basement-key captures continue to cover the adjacent story states
- Readable-book captures — `output/playwright/mr-feast-readable-books/titled-book-spines-desktop.png`, `titled-book-spines-reading-room-desktop.png`, `titled-book-spines-archive-desktop.png`, `printed-book-with-handwritten-marginalia-desktop.png`, and `readable-book-mobile.png` cover the subtle matte-black physical titles across all three collections, the disguised XIII copy, ordinary printed prose, and the angled hurried handwritten note; the basement-key suite adds `printed-book-marginalia-mobile.png`
- Tamper and speech captures — `output/playwright/mr-feast-tamper-distractions/tilted-portrait-desktop.png`, `pulled-chair-desktop.png`, `noticed-bubble-desktop.png`, `speech-bubble-desktop.png`, and `speech-bubble-mobile.png` confirm the visible frame roll with straighten prompt, the diagonal dining chair against its aligned row, and the readable italic bubble over Mr. Feast's head at 1280×820 and 390×844
- `node scripts/test-mr-feast-workroom-code-clue.mjs` — passed the scratch-table/keypad-code agreement, carrier-only `0.32rad` roll with the ordinary `0.17rad` tilt preserved, alternating raised-corner alignment, at least 94% inner-edge geometric exposure on all four marks, manual non-font gouge paths, four hidden-by-default scratches with tamper-entry `artId`s, real E-key reveal with a neutral toast and evolving `Painting Room notes` entry, straighten and Mr.-Feast-fix re-hides with persistent discovery, full raw-mark completion without a player-facing assembled answer or keypad/Workroom/PIN/code wording, mid-hunt save/load persistence with visibility resync, reversible Dev Mode grants, the unlocked-keypad digit-echo regression, desktop/mobile render evidence, and zero console errors
- Keycode scratch captures — `output/playwright/mr-feast-workroom-code-clue/scratch-{five-doors,polite-eclipse,garden-knees,choir-floorboards}-corner-desktop.png` show every complete rough numeral–digit mark under its raised frame corner at 1280×820; `scratch-five-doors-corner-mobile.png` confirms the phone close view, while `scratch-revealed-desktop.png` and `unlocked-keypad-echo-desktop.png` retain the discovery and accepted-pad proofs.
- Contestant conversation captures — `output/playwright/mr-feast-contestant-conversations/mara-voss-desktop.png`, `kip-solano-desktop.png`, `juniper-cross-desktop.png`, and `kip-solano-mobile.png` confirm three distinct silhouettes, occupied rooms, animated poses, named dialogue bubbles, and a touch-safe phone layout at 1280×820 and 390×844
- Caught-pursuit captures — `output/playwright/mr-feast-caught-pursuit/game-over-desktop.png` and `game-over-mobile.png` confirm the oxblood `CAUGHT` dialog with themed copy and working Load/Start-over controls over the frozen scene at 1280×820 and 390×844

## Next action

For the basement flashlight, enter through the Kitchen service stair and confirm the warm-lensed wall cradle is obvious on the Archive-side landing without searching shelves. Take it with E/touch, then use F or `Light` in a dark Archive aisle: judge whether the center beam reveals a useful route while the periphery still feels unsafe, watch the crouched stealth meter fall, and confirm `Flashlight spotted` plus Mr. Feast's investigation creates pressure without an immediate unavoidable chase. Repeat once behind a solid Workroom/Archive obstruction to confirm an occluded camera does not invent an alert, then save/load and confirm ownership remains while the beam returns off.

For Tardigrade: Micro Mayhem, play the Petri Dish on desktop or phone and judge the authored hero's silhouette plus `idle`, `scuttle`, `dash`, `curl`, and `airborne` feel at normal camera distance. Approach the rotifer, ciliate, and waterbearling, then interact with algae, bacteria, a droplet, and pollen. Confirm each reads more clearly than the procedural version, stays grounded and correctly oriented, and preserves the original handling. Open the Micro Store, equip a non-classic skin plus head/face/back cosmetics, and confirm the tint and Blender-authored sockets move naturally with the rig.

For the targeted visual playtest, confirm the Feast Says rig reads as a slim long-lens movie/broadcast camera at the left of the contestant lineup on desktop and phone, remains a couple of feet back from them, and keeps both lamp faces and the lens on the players throughout a round. In Storm Run, confirm Mr. Feast turns to meet the player immediately after Interact, stays visibly lit only through `3–2–1`, and returns to storm darkness at the west-facing `Run!` release. As the player turns south into the maze's long final straight, confirm the far end is dark until lightning reveals Mr. Feast facing north toward the player just before the westward exit turn, then relights only after he disappears.

User completes Feast Says and reports to Storm Run without QA shortcuts. At the call, confirm the rear-terrace sign is gone; the long-lens cinema/broadcast rig, two studio lights, and boom microphone still read clearly as a film set without crowding Mr. Feast; and the host waits about three metres out from the threshold facing back toward the open door. Verify that only facing and interacting with him begins the briefing, that the five-minute timer pauses behind blocking UI, and that ignoring the call produces a fair no-show elimination. Confirm Mr. Feast visibly brightens and faces the player for the rules and `3–2–1`, then returns to the darker storm treatment as he says only `Run!` and releases the race. He remains silent at every checkpoint while the blue guides and chimes make the route obvious. At the north end of the garden where it meets the front yard, look away or down while crossing, then turn toward the northwest trees; when close to the front door, face toward the northeast trees; as the route turns south into the long final maze straight, verify its far end is dark before the reveal and face down the corridor. Each time, confirm the bolt waits until Mr. Feast is actually visible, then produces one loud recorded thunder roll with no static-like snap and a bright flash without a first-strike frame hitch, leaves runners/checkpoints present, shows a human-sized Mr. Feast rather than a tree-sized figure, and removes him with darkness. For the last reveal, confirm he is at the far end immediately before the westward exit turn, facing north toward the player, and that the exit light returns only after he disappears. Confirm there are exactly three non-hazard appearances, then win and check that the ending moves to the back door, Mara covers her face with both hands and delivers her complete sad line, and Juniper visibly walks inside. Step just inside the back door and confirm Mr. Feast remains solidly visible and staged; then move at least sixteen metres away with the terrace out of view and confirm Mara disappears and Mr. Feast resumes patrol. Deliberately lose once to recheck the alternate outcome.

User starts a fresh mansion run and triggers Feast Says by finding either the XIII shelf book, concealed garden shovel, or first Painting Room scratch. Confirm the clue remains understandable before the live call, no idle countdown strip appears during free exploration, and the Ballroom sign is replaced by a clearly readable long-lens cinema/two-light/boom film set with Mr. Feast unobstructed at its center. Verify the camera remains farther from the lineup, its lens and both light faces stay on the player/contestant marks once the game begins, and only facing and interacting with Mr. Feast begins. Confirm the five-minute timer pauses behind blocking UI, and ignoring the call produces a fair no-show elimination. Play the fixed sequence without QA shortcuts: obey `Feast says step left`, hold still for plain `Step right`, obey `Feast says step back`, hold still for plain `Crouch`, look at a contestant and press E or touch Interact for distrust, then physically move toward the contestant you would sacrifice. Confirm every rule, command, and verdict comes from Mr. Feast's bubble while the UI shows only event, round, and time. Judge whether both fake-outs, each 7.5-second response window, and each 7.2-second result hold feel readable; whether the verdict, anxious/funny contestant line, and warning hand off naturally; and whether the two choices are clear without direction-to-name mappings. After winning, stay for Kip's plea and Mr. Feast's ominous answer, then verify survivor debriefs and offscreen cleanup. Confirm a surviving run restores the clue trail, phone controls remain comfortable, and a deliberate loss or tie gives a fair `ELIMINATED` recovery.

User starts a fresh mansion run, crosses the threshold, and reads the complete seven-line welcome without pressing anything once to confirm the automatic pace feels comfortable. Repeat with E or the touch prompt to confirm optional continuation never feels accidental, and judge whether Mr. Feast's one-million-dollar pitch, house rules, basement prohibition, and final contestant threat land as suspicious and cryptic enough on desktop or phone.

User approaches a chair and sofa on desktop or phone, confirms Sit/Stand up feels natural and remains vulnerable, then visits Mara, Kip, and Juniper long enough to see their mostly seated cadence, subtle seated breathing/glances, occasional short hangout walks to genuinely different spots, relaxed arms-down standing poses, and smooth furniture transitions. Re-check Juniper from the reported three-quarter sofa angle, Mara at the Library writing chair, and Kip at the Ballroom sideline chair: each lowered cushion should sit below the contestant thigh line while hips remain supported and roots, knees, and boots stay naturally floor-aligned. From front and side, confirm their seated palms lie flat over—not inside or visibly above—the upper thighs, and watch one short walk from each contestant to confirm the palms face inward rather than forward or away from the body.

User enters the temporary PIN `0513`, confirms number keys visibly populate the Workroom pad, and checks that both the west operator/monitor half and east server bank stay readable while walking around the camera-free room. Super Slop's six new fighters and 24 ability animations, the widened upper-window gallery, grounded-gait pacing, estate-statue approval, camera warning/search tension, Music Room spacing, sprint/crouch feel, Mr. Feast facial approval, and the unguided Library shelf-book discovery remain separate playtests.

For Super Slop mobile, user opens the game at phone size, starts a match, taps Max in portrait and landscape, confirms every movement/combat button stays comfortable, then exits Max and checks that the embedded match returns intact.

For mansion books, user checks that spine titles stay legible in the Library, Reading Room, and Archive; opens several ordinary volumes; then reads the slightly pulled XIII copy and confirms it looks like a normal printed catalog book that someone secretly annotated by hand on desktop or phone.

For mansion audio, user walks between the wood floors, basement stone, and wet grounds; crouches and sprints; opens doors/books and uses switches/keypads; then checks that rain exposure, thunder weight, step cadence, and interaction loudness stay atmospheric without becoming tiring.

For mansion mischief, user tilts a portrait, pulls a dining chair diagonal, and leaves the refrigerator open; watches Mr. Feast walk over and correct each while reading his overhead reaction from across the room and from inside the coat closet; then speaks with him directly (and again mid-errand) and checks that the bubble stays legible at a distance on desktop and phone without feeling spammy.

For the Painting Room marks, user tilts each of the four scratch-carrier paintings (east `Five Doors`, west `A Very Polite Eclipse`, north `The Garden Has Too Many Knees`, south `The Choir Beneath the Floorboards`), confirms a pale numeral-and-digit scratch appears under each frame with a neutral discovery toast and a growing `Painting Room notes` Tab entry, lets Mr. Feast straighten one to confirm the note survives, then independently infers what the completed raw marks open and tests the resulting digits at the Workroom pad — also confirming digits visibly echo even if the pad was already unlocked from an older save.

For the stealth meter, user crouches with `C` in a lit room and confirms the meter appears above the energy HUD and reads mid-range, crouch-walks and watches it fall then recover after stopping, flips the room lights off (or slips into an unlit room) and confirms it climbs toward the 90s with the `Stealth · dark` label, then tests the payoff: tilt a portrait about 4–5 meters in front of Mr. Feast from a dark motionless crouch and confirm he does not react, repeat standing in light to confirm he still catches it instantly, and walk right up to him crouched in the dark to confirm point-blank mischief is always seen. During a chase, duck into a dark room, crouch, and hold still to confirm he loses the trail faster than when standing.

For getting caught, user tilts a portrait right in front of Mr. Feast and walks (not sprints) away to confirm he can be outpaced, lets him catch up for the warning and confirms the warning bubble lingers and is not immediately replaced by a "fixing the picture" visit, repeats the mischief under a solid-red camera to draw the recorded pursuit, then saves, heads into the unlocked basement and lets a basement camera lock on (or lets Mr. Feast spot them in person down there) to confirm the trespass chase starts with no tampering needed, gets caught deliberately, and checks that the `CAUGHT` screen, Load last save, and Start over all feel fair and readable. Along the way, user watches whether his chase route feels direct (cutting across rooms rather than looping the house) and whether he keeps chasing as long as he can still see you.

For the new cast, user visits Mara in the Library, Kip in the Ballroom, and Juniper in the upper Reading Room; speaks to each several times on desktop or phone; and checks that their silhouettes, idle gestures, room placement, and strategist/comic/folklorist voices feel distinct without blocking circulation.

## Working conventions

- Preserve the current static Three.js/Blender GLB architecture from ADR 0001.
- Preserve Super Slop Brothers' static Canvas2D runtime; Meshy/Blender stay offline and sprite atlases remain a visual layer over existing gameplay authority.
- Keep the three unrelated `.rainbot-*-state.json` files untouched.
- Do not commit or push this milestone unless the user asks.
